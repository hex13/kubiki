{
  "name": "kubiki",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "esbuild --bundle --outfile=src/out.js src/example.js --watch"
  },
  "author": "Łukasz Lityński",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
